<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Express Dev Skills</title>
</head>
<body>
  <h1>Skills</h1>
  <ul>
    <% skills.forEach(skill => { %>
      <li><%= `Skill: ${skill.description}` %></li>
      <li><%= `Skill Level: ${skill.skillLevel}` %></li>
    <% }) %>
  </ul>
  <a href="/">Back</a>
</body>
</html>